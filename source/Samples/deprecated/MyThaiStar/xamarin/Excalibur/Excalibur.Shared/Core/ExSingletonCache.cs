namespace Excalibur.Shared.Core
{
    /// <inheritdoc />
    /// <summary>
    /// Example singleton object that can be used for caching.
    /// Might give this some useful implementation at some point ;)
    /// </summary>
    public class ExSingletonCache : ExSingletonBase<ExSingletonCache>
    {
        // for example settings can be included here as a singleton lol
    }
}
